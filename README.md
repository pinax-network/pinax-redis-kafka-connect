# pinax-redis-kafka-connect
This is a simple Kafka Connect plugin that reads messages from a Kafka topic and writes them to a Redis.

## Package
To build the package, run the following command:
```bash
mvn clean package
```